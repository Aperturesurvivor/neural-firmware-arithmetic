# Phase 7 paper

Compile from the repository root with:

```bash
tectonic paper_phase7/main.tex --outdir paper_phase7/build
```

The compiler writes `paper_phase7/build/main.pdf`. The reviewed, tracked copy is
`deterministic-neurons-qwen.pdf`.

The minimal arXiv source, anonymous MATH-AI 2026 workshop version, copy-ready
metadata, and upload checklist are staged in `../publication/`. Those files do
not perform or imply a public submission.

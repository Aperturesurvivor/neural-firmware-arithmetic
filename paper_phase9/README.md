# Phase 9 report

Compile from the repository root with the bundled LaTeX helper:

```bash
python3 \
  /Users/josiahwilson/.codex/plugins/cache/openai-bundled/latex/0.2.4/scripts/compile_latex.py \
  /Users/josiahwilson/Josiah/Project-Workspace/neural-firmware-arithmetic/paper_phase9/main.tex \
  --output-directory \
  /Users/josiahwilson/Josiah/Project-Workspace/neural-firmware-arithmetic/paper_phase9/build
```

The reviewed tracked copy is
`deterministic-neurons-interface-hardening.pdf`.

This report must retain the frozen Phase 9 compound verdict and every failed
gate. It must distinguish semantic routing, typed operand recovery,
deterministic calculator execution, and downstream decoding.
